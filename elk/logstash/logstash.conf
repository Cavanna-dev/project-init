input {
  file {
    type => "nginx_access"
    path => "/home/elk/nginx/symfony_access.log"
    start_position => beginning
  }
  file {
    type => "nginx_error"
    path => "/home/elk/nginx/symfony_error.log"
    start_position => beginning
  }
  file {
    type => "symfony_dev"
    path => "/home/elk/symfony/dev.log"
    start_position => beginning
  }
  file {
    type => "symfony_prod"
    path => "/home/elk/symfony/prod.log"
    start_position => beginning
  }
}

filter {
  if [type] == "nginx_access" {
    grok {
      patterns_dir => "./patterns"
      match => { "message" => "%{NGINXACCESS}"}
    }
  }
  else if [type] in ["symfony_dev", "symfony_prod"] {
    grok {
      patterns_dir => "./patterns"
      match => { "message" => "%{SYMFONY}"}
    }
  }
}

output {
  elasticsearch {
    host => "localhost"
    cluster => "logstash"
  }
}
